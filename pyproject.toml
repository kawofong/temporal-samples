[project]
name = "temporal-samples"
version = "0.1.0"
description = "A mono-repo of Temporal workflows"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic==2.11.4",
    "pytz==2025.2",
    "temporalio==1.11.1",
]
